Bien. Veo el sitio funcionando. Ahora voy a crear el **PROMPT PRÁCTICO** adaptado a tu stack real (React+Vite+Express):

***

# 🎯 **PROMPT PRÁCTICO GRUPOTRANSERVICA.COM**
## **Implementación en React+Vite+Express para SEO Venezuela**

### **MISIÓN: TOP 1 en Google Venezuela + 5 Blogs/Día Automáticos**

***

## **1. CONTEXTO Y OBJETIVOS (ADAPTADO A TU REALIDAD)**

El proyecto **gruasequiser.com** está funcionando bien con:
- ✅ React 18 + Vite (Frontend SPA)
- ✅ Express.js (Backend)
- ✅ Drizzle ORM + PostgreSQL
- ✅ Sistema de blogs bilingüe
- ✅ Formularios de contacto automáticos

**TAREA**: Replicar este mismo stack para **grupotranservica.com** (transporte carga pesada, capacidad 100-500 ton) y agregar:
1. **Sistema automático 5 blogs/día** con OpenAI API
2. **200+ keywords geo-específicos** (8 ciudades × 6 sectores)
3. **Sitemap dinámico + Schema markup SEO**
4. **Landing pages dinámicas** por ciudad/sector
5. **Analytics mejorado** (Posthog/GA4)

**Tiempo estimado**: 3-4 semanas en Replit

***

## **2. ESTRUCTURA TÉCNICA (REACT+VITE+EXPRESS NATIVA)**

### **Frontend (React+Vite)**
```
grupotranservica-frontend/
├── src/
│   ├── components/
│   │   ├── Hero.tsx
│   │   ├── Services.tsx
│   │   ├── BlogFeed.tsx
│   │   ├── ContactForm.tsx [Ya existe]
│   │   ├── DynamicCityPage.tsx ← NUEVO
│   │   └── QuoteCalculator.tsx ← NUEVO
│   ├── pages/
│   │   ├── Home.tsx
│   │   ├── Blog.tsx
│   │   ├── BlogPost.tsx
│   │   ├── Contact.tsx
│   │   ├── [city]/[sector].tsx ← NUEVA RUTA DINÁM
ICA
│   │   └── Servicios.tsx
│   ├── lib/
│   │   ├── seoKeywords.ts ← 200+ KEYWORDS
│   │   ├── blogTemplates.ts ← PLANTILLAS BLOGS
│   │   └── api.ts [Llamadas a Express]
│   └── App.tsx
└── vite.config.ts
```

### **Backend (Express)**
```
grupotranservica-backend/
├── src/
│   ├── routes/
│   │   ├── api/
│   │   │   ├── contacto.ts [Usar existente]
│   │   │   ├── blogs.ts ← NUEVO (CRUD)
│   │   │   ├── blogs-generate.ts ← NUEVO (5/día)
│   │   │   ├── sitemap.ts ← NUEVO
│   │   │   ├── og-images.ts ← NUEVO
│   │   │   ├── keywords.ts ← NUEVO
│   │   │   └── analytics.ts ← NUEVO
│   │   └── pages.ts [Existente]
│   ├── services/
│   │   ├── blogGenerator.ts ← NUEVO (OpenAI)
│   │   ├── seoService.ts ← NUEVO
│   │   └── emailService.ts [Existente]
│   ├── db/
│   │   └── schema.ts [Extender con tabla blogs]
│   ├── tasks/
│   │   └── generate5BlogsDaily.ts ← NUEVO (Cron)
│   └── server.ts
├── .env [Agregar OPENAI_API_KEY]
└── package.json
```

### **Base de Datos (PostgreSQL + Drizzle)**
```typescript
// db/schema.ts - AGREGAR TABLA BLOGS

export const blogs = pgTable('blogs', {
  id: serial('id').primaryKey(),
  title: text('title').notNull(),
  slug: text('slug').notNull().unique(),
  content: text('content').notNull(),
  excerpt: text('excerpt'),
  
  // SEO
  metaTitle: text('meta_title'),
  metaDescription: text('meta_description'),
  keywords: text('keywords').array(), // ['keyword1', 'keyword2']
  
  // Generación automática
  city: text('city'), // 'caracas', 'maracaibo', etc
  sector: text('sector'), // 'petrolero', 'petroquimico', etc
  template: text('template'), // 'CityGuide', 'PriceGuide', etc
  
  // Imágenes
  coverImage: text('cover_image'),
  ogImage: text('og_image'),
  
  // Control
  published: boolean('published').default(false),
  autoGenerated: boolean('auto_generated').default(false),
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
  publishedAt: timestamp('published_at'),
});
```

***

## **3. SISTEMA DE GENERACIÓN DE BLOGS AUTOMÁTICO (5/DÍA)**

### **A. Estructura TypeScript**

```typescript
// src/services/blogGenerator.ts

import OpenAI from 'openai';
import { blogs } from '../db/schema';

interface BlogTemplate {
  name: string;
  titleTemplates: string[];
  slugPatterns: string[];
  contentOutline: string[];
  keywords: string[];
}

const BLOG_TEMPLATES: BlogTemplate[] = [
  {
    name: 'CityGuide',
    titleTemplates: [
      'Transporte de {sector} en {city}: Guía Completa {year}',
      'Logística {sector} en {city}: Servicios y Contacto',
      'Carga Pesada en {city}: Expertos en {sector}',
    ],
    slugPatterns: [
      'transporte-{sector}-{city}',
      'logistica-{sector}-{city}',
      'carga-pesada-{city}-{sector}',
    ],
    contentOutline: [
      'Introducción: Por qué {city} es centro de {sector}',
      'Desafíos logísticos en {city}',
      'Soluciones Grupo Transervica',
      'Casos de éxito',
      'Capacidades (100-500 toneladas)',
      'Cobertura nacional',
      'Contacto y cotización',
    ],
    keywords: [
      'transporte {sector} {city}',
      'logística {sector} {city}',
      'carga pesada {city}',
    ],
  },
  
  {
    name: 'PriceGuide',
    titleTemplates: [
      '¿Cuánto cuesta transporte en {city}? Precios 2025',
      'Tarifas transporte {sector} en {city}',
      'Presupuesto logística {city}: Cálculo exacto',
    ],
    slugPatterns: [
      'precio-transporte-{city}-2025',
      'tarifas-{sector}-{city}',
      'presupuesto-logistica-{city}',
    ],
    contentOutline: [
      'Factores que afectan precios',
      'Tabla de tarifas',
      'Calculadora de presupuesto',
      'Descuentos por volumen',
      'Formas de pago',
    ],
    keywords: [
      'precio transporte {city}',
      'tarifa {sector} {city}',
      'presupuesto {city}',
    ],
  },

  {
    name: 'CaseStudy',
    titleTemplates: [
      'Caso de éxito: Transporte {sector} en {city}',
      'Proyecto: Movilización de {tons} toneladas en {city}',
      'Testimonio: Cómo resolvimos {challenge}',
    ],
    slugPatterns: [
      'caso-exito-{sector}-{city}',
      'proyecto-{city}-{sector}',
      'testimonio-{city}',
    ],
    contentOutline: [
      'Presentación del cliente',
      'Desafío específico',
      'Solución propuesta',
      'Proceso y timeline',
      'Resultados obtenidos',
    ],
    keywords: [
      'transporte exitoso {city}',
      'logística profesional {sector}',
      'carga pesada {city}',
    ],
  },
  
  {
    name: 'ServiceHighlight',
    titleTemplates: [
      'Lowboy en {city}: Transporte 100-500 toneladas',
      'Almacenamiento industrial en {city}: Seguro y confiable',
      'Consultoría logística {city}: Optimiza tu operación',
    ],
    slugPatterns: [
      'lowboy-{city}',
      'almacenamiento-{city}',
      'consultoría-{city}',
    ],
    contentOutline: [
      'Descripción del servicio',
      'Ventajas principales',
      'Especificaciones técnicas',
      'Casos de aplicación',
      'Cómo contratar',
    ],
    keywords: [
      'lowboy {city}',
      'almacenamiento {city}',
      'consultoría {city}',
    ],
  },

  {
    name: 'SectorDeep',
    titleTemplates: [
      'Logística {sector} en Venezuela: Soluciones especializadas',
      'Transporte {sector}: Requisitos y regulaciones {year}',
      'Sector {sector} en {city}: Oportunidades logísticas',
    ],
    slugPatterns: [
      'logistica-{sector}-venezuela',
      'transporte-{sector}-regulaciones',
      'sector-{sector}-{city}',
    ],
    contentOutline: [
      'Características del sector {sector}',
      'Desafíos específicos',
      'Requisitos regulatorios',
      'Soluciones adaptadas',
      'Experiencia Transervica',
    ],
    keywords: [
      'logística {sector}',
      'transporte {sector} venezuela',
      '{sector} especializado',
    ],
  },
];

const CITIES = ['caracas', 'maracaibo', 'valencia', 'barcelona', 'puerto-ordaz', 'puerto-cabello', 'barquisimeto', 'punto-fijo'];
const SECTORS = ['petrolero', 'petroquimico', 'siderurgico', 'energetico', 'portuario', 'manufacturero'];

async function generateBlogContent(
  template: BlogTemplate,
  city: string,
  sector: string
): Promise<{ title: string; slug: string; content: string }> {
  const client = new OpenAI({
    apiKey: process.env.OPENAI_API_KEY,
  });

  // Elegir título y slug aleatorios del template
  const titleTemplate = template.titleTemplates[
    Math.floor(Math.random() * template.titleTemplates.length)
  ];
  const slugPattern = template.slugPatterns[
    Math.floor(Math.random() * template.slugPatterns.length)
  ];

  const title = titleTemplate
    .replace('{sector}', sector)
    .replace('{city}', city)
    .replace('{year}', new Date().getFullYear().toString());

  const slug = slugPattern
    .replace('{sector}', sector)
    .replace('{city}', city);

  // Generar contenido con OpenAI
  const prompt = `
  Escribe un artículo de blog profesional para una empresa de transporte de carga pesada (Grupo Transervica).
  
  Título: ${title}
  Palabras clave: ${template.keywords.join(', ')}
  
  El artículo debe:
  1. Tener 800-1200 palabras
  2. Estar optimizado para SEO (incluir palabras clave naturalmente)
  3. Incluir secciones claras (H2, H3)
  4. Tener un CTA al final
  5. Ser profesional y formal (B2B)
  6. Enfocado en ${sector} en ${city}
  
  Secciones sugeridas:
  ${template.contentOutline.map((s) => `- ${s}`).join('\n')}
  
  Formato: HTML simple (sin tags <html>, solo divs/p/h2/h3)
  `;

  const response = await client.chat.completions.create({
    model: 'gpt-4',
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.7,
    max_tokens: 2000,
  });

  const content = response.choices[0]?.message.content || '';

  return { title, slug, content };
}

async function generate5BlogsDaily() {
  const randomTemplates: BlogTemplate[] = [];

  // Seleccionar 5 templates aleatorios
  for (let i = 0; i < 5; i++) {
    const template = BLOG_TEMPLATES[Math.floor(Math.random() * BLOG_TEMPLATES.length)];
    randomTemplates.push(template);
  }

  // Para cada template, generar con city/sector aleatorio
  for (const template of randomTemplates) {
    const city = CITIES[Math.floor(Math.random() * CITIES.length)];
    const sector = SECTORS[Math.floor(Math.random() * SECTORS.length)];

    const { title, slug, content } = await generateBlogContent(template, city, sector);

    // Generar keywords
    const keywords = template.keywords.map((kw) =>
      kw.replace('{sector}', sector).replace('{city}', city)
    );

    // Meta description
    const metaDescription = content.substring(0, 155); // Primeras 155 caracteres

    // Guardar en BD
    await db.insert(blogs).values({
      title,
      slug,
      content,
      metaDescription,
      keywords,
      city,
      sector,
      template: template.name,
      autoGenerated: true,
      published: true,
      publishedAt: new Date(),
      coverImage: `https://via.placeholder.com/1200x630?text=${encodeURIComponent(title)}`,
    });

    console.log(`✅ Blog generado: ${title}`);
  }
}

export { generate5BlogsDaily, generateBlogContent };
```

### **B. Cron Job (Ejecutar 5/día automáticamente)**

```typescript
// src/tasks/generate5BlogsDaily.ts

import cron from 'node-cron';
import { generate5BlogsDaily } from '../services/blogGenerator';

// Ejecutar todos los días a las 9 AM
export function setupBlogGenerationSchedule() {
  cron.schedule('0 9 * * *', async () => {
    console.log('🤖 Iniciando generación de 5 blogs diarios...');
    try {
      await generate5BlogsDaily();